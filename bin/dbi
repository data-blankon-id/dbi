#!/usr/bin/env node

var program = require('commander');

program
  .version('1.0.0')
  .command('start', 'Start blankon data id service')
  .command('list', 'List all available api modules')
  .command('add [name]', 'Add a data endpoint from npm')
  .parse(process.argv);
